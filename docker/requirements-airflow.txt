# Extra Python deps installed on top of the official Airflow image.
# (Airflow already ships SQLAlchemy; we add the rest the pipeline needs.)
pyspark==3.5.1
boto3==1.34.131
requests==2.32.3
pandas==2.2.2
psycopg2-binary==2.9.9
